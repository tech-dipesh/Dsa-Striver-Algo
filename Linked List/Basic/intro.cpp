#include <iostream>
#include <vector>
using namespace std;
int main(){
   int b=823;
   int *c=&b;
   cout<<c; 
  //  Which it gives me the b memory location the same approach that we use on linked list.
  // We can't store memory location rather we store teh pointer to the location.
return 0;
}