The level order is called the bfs traversal as it go to the level wise access teh first, entire level and then goes to next level.

On the post order traversal there is the two answer of the post order traversal.


Check minor things on the postorder traversal using the two iterative solution of the stack.


Postorder is i can done with 2 stack, using preorder approach with first push left, and theen just reverse, but striver is little complex i feel but those 3 will be sufficient don't need overcomplex. 



With the one pass three traversal, i've done that with the stack pair approach,
GEt top element and pop top element
if count is 1 then store on preorder, and increametn count to 2,
if count is 2 stoer ron Intorder, move to right if exist,
else it's a postorder, store 


Find max height of teh binary tree:
The approach is the just keep going to the left, and right each time and with return teh max of the left or right with the +1 value.





For the balanced binary tree:
The appraoch is just same as the, max height of the binary tree, with 